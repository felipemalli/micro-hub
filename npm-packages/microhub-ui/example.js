// Exemplo simples de como usar o @microhub-ui/components

// Em um projeto React:
/*
import { Button } from '@microhub-ui/components';

function App() {
  return (
    <div>
      <Button variant="primary" onClick={() => alert('Clicou!')}>
        Meu Botão
      </Button>
      
      <Button variant="danger" size="lg" loading>
        Carregando...
      </Button>
      
      <Button variant="secondary" disabled>
        Desabilitado
      </Button>
    </div>
  );
}
*/

console.log('Para usar este package:');
console.log('1. npm install @microhub-ui/components');
console.log('2. import { Button } from "@microhub-ui/components"');
console.log('3. Use o componente: <Button variant="primary">Texto</Button>');